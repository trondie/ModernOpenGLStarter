#pragma once

#include <glad/glad.h>
#include <string>

const int windowWidth = 1400;
const int windowHeight = 768;
const std::string windowTitle = "Basic Modern OpenGL starter";
const GLint windowResizable = GL_TRUE;
const int windowSamples   = 1;
